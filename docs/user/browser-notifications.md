# Browser completion notifications

T3 Code can show a system notification when an agent finishes a turn while the browser tab is in the background.

Open **Settings → General** and enable **Browser completion notifications**. Your browser will ask for permission the first time. If notifications are blocked, enable them for T3 Code in your browser or operating-system notification settings.

Clicking a completion notification focuses T3 Code and opens the thread that finished.
